using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class VisualNovel : MonoBehaviour
{
    /*Button[] buttons = FindObjectsOfType<Button>();

    void Start()
    {
        foreach (Button button in buttons)
        {

        }
    }

    private void assignButton(Button button)
    {
        if (button.name == "MainMenuButton")
        {
            button.onClick.AddListener(ToMainMenu);
        }
        else if (button.name == "GameplayButton")
        {
            button.onClick.AddListener(ToGameplay);
        }
    }

    public void ToMainMenu()
    {
        Debug.Log("Sending to menu");
        Loader.Load(Loader.Scene.MainMenuDummy);
    }

    public void ToGameplay()
    {
        Debug.Log("Sending to gameplay");
        Loader.Load(Loader.Scene.GPdummy);
    }*/
}
